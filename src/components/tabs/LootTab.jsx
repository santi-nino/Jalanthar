// REMOVED (v7.0): the Loot generator was deleted per the DM's explicit
// call -- "I didn't like the loot generator at all. It was a failed
// experiment. Delete it." Nothing in the app imports this file anymore
// (see App.jsx / Sidebar.jsx / DataContext.jsx, all cleaned up in the
// same round). It's left here as an empty stub, rather than actually
// removed from disk, only because this particular file's slot in the
// delivered project folder can't be deleted outright from this session --
// the packaged zip you download has it actually gone.
export default function LootTab() {
  return null
}
