// Scratch verification script used once during development to confirm the
// Pantheon tab's generation-row layout no longer fragments a wide
// generation into multiple fake-looking rows, and that rows center
// correctly against the widest row in their pantheon. Not part of the
// app, not imported by anything, and not included in the packaged zip you
// download -- left here as an empty stub only because this file's slot in
// this particular session's project folder can't be deleted outright.
export {}
