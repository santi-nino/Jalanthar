// Scratch verification script used once during development to confirm the
// Pantheon tab's capped-width, generation-labeled layout keeps every
// pantheon's max row width bounded (fixing the "one illegible 23-card
// line" bug from the previous layout) while still keeping sub-rows of the
// same generation visually distinct from the next real generation. Not
// part of the app, not imported by anything, and not included in the
// packaged zip you download -- left here as an empty stub only because
// this file's slot in this particular session's project folder can't be
// deleted outright.
export {}
